[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dr-docsem"
version = "0.1.0"
description = "DocSem document-grounded quantitative reasoning pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
